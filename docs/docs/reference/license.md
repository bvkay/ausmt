# License

The AusMT **framework**, everything in this repository other than the survey data AusMT serves, is
licensed under the Apache License 2.0. The `NOTICE` file in the repository root is the normative
statement of that scope, and `LICENSE` carries the licence text.

## Framework, not data

Apache-2.0 covers the framework, not the data. The magnetotelluric survey data AusMT processes and
serves (transfer functions and survey metadata) is licensed individually by its custodians (typically
CC-BY-4.0) and lives in the separate `ausmt-surveys` repository; each survey package carries its own
`LICENSE.md`. The sample survey bundled under `engine/data/` for testing is CC-BY-4.0.

When you reuse survey data discovered through AusMT, follow the licence and attribution terms of the
originating survey, not the portal software licence.

Copyright 2026 AuScope and the AusMT contributors.
