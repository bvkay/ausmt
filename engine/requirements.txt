# AusMT core build/runtime requirements (loose floors).
#
# The single canonical pipeline:
#
#   extract/build_portal.py
#
# parses EDIs/MTH5 with mt_metadata/mth5 (the USGS community library), the SOLE engine (see the
# extractor note at the top of extract/build_portal.py). The build hard-exits without it.
#
# These are loose floors (the tested majors). For a PINNED, reproducible install — what CI uses —
# install the full transitive closure on Python 3.12:
#
#   pip install -r environments/requirements-mtmetadata-lock.txt
#
# Optional layers:
#   Development / tests:   pip install -r requirements-dev.txt
#   Tier-3 decomposition:  the MTpy-v2 fork, pinned to a commit (see the build-products workflow)

numpy>=1.24
mt_metadata>=1.0
mth5>=0.6
PyYAML>=6   # survey.yaml / portal.config.yaml parsing; a DIRECT dependency, never a transitive one. The stdlib _mini_yaml stays as a fallback.
ruamel.yaml>=0.18   # The curator metadata editor round-trips survey.yaml edits (comments + unknown keys preserved byte-for-byte). Runs in the gw-runner (this engine image) ONLY - NEVER the gateway, which never parses survey content.
pillow>=10.1   # per-survey link-preview cards (pages/og/*.png). SOFT dependency: extract/_pages gates on importability and falls back to the root card, so a pillow-less environment still builds every page.
