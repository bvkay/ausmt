# AusMT development and test requirements.
#
# Pulls in the core runtime (mt_metadata/mth5/numpy — the build engine) plus the test tooling.
# For the PINNED, reproducible engine (what CI runs), also install the lock on Python 3.12:
#   pip install -r environments/requirements-mtmetadata-lock.txt

-r requirements.txt

pytest==8.2.0
pytest-xdist==3.8.0   # parallel test runs: both CI engine lanes run `pytest -n 4` (4-vCPU runners).
                      # Installed -c the lock everywhere it matters (engine.Dockerfile, build-products.yml),
                      # so its one dependency (execnet) can never move the science stack.
jsonschema>=4    # mtcat.schema.json validation in tests
rfc3339-validator>=0.1.4   # activates jsonschema FormatChecker's date-time check; without it the
                           # MTCAT 2.0 format-checking gates (malformed generated_at) pass vacuously.
                           # test_mtcat_schema_v20._validator asserts the checker is genuinely active.
ruff==0.15.17    # lint gate (broad E/F/PL/BLE; config + documented ignores in pyproject.toml [tool.ruff])
